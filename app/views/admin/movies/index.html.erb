<% @movies.each do |m| %>
  <p><%= m.name %></p>
  <p><%= m.year %></p>
  <p><%= m.description %></p>
  <%= image_tag "#{m.image_url}" %>
  <p><%= m.is_showing %></p>
  <p><%= m.created_at %></p>
  <p><%= m.updated_at %></p>
<% end %>