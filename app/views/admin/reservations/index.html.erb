<h2>予約一覧</h2>
<% if @reservations.present? %>
  <table>
    <tr>
      <td>ID</td>
      <td>予約者氏名</td>
      <td>メールアドレス</td>
      <td>映画タイトル</td>
      <td>予約時間</td>
      <td>スクリーン</td>
      <td>座席番号</td>
      <td>作成日時</td>
      <td>更新日時</td>
      <td>編集・削除</td>
    </tr>
  <% @reservations.each do |r| %>
    <tr>
      <td>
        <%= r.id %>
      </td>
      <td>
        <%= r.name %>
      </td>
      <td>
        <%= r.email %>
      </td>
      <td>
        <%= r.schedule.movie.name %>
      </td>
      <td>
        <%= l r.schedule.start_time,format: :short %> ~ <%= l r.schedule.end_time,format: :short %>
      </td>
      <td>
        <%#= %>
      </td>
      <td>
        <%= r.sheet.row %> - <%= r.sheet.column %>
      </td>
      <td>
        <%= l r.created_at, format: :long %>
      </td>
      <td>
        <%= l r.updated_at, format: :long %>
      </td>
      <td>
        <%#= r. %>
      </td>
    </tr>
  <% end %>
  </table>
<% else %>
  本日以降の予約はありません
<% end %>
