<% if @movie.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>